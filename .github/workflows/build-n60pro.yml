name: Netcore N60Pro 高功率24.10(全插件版)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false

    steps:
      - name: 检出 ImmortalWrt 源码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 清理磁盘空间
        run: |
          sudo rm -rf /usr/local/lib/android /opt/ghc /usr/share/dotnet
          docker rmi $(docker images -q) -f || true
          sudo apt clean
          df -h

      - name: 安装编译依赖
        run: |
          sudo apt update
          sudo apt install -y build-essential ccache ecj fastjar file g++ gawk \
          gettext git java-propose-classpath libelf-dev libncurses-dev \
          libssl-dev unzip python3 python3-setuptools wget \
          python3-distutils rsync subversion swig time xsltproc zlib1g-dev \
          quilt libfuse-dev

      # ===== feeds 配置（全部插件通过 feeds 源引入） =====
      - name: 配置 feeds（包含所有外部插件源）
        run: |
          if [ ! -f feeds.conf ]; then
            cp feeds.conf.default feeds.conf 2>/dev/null || touch feeds.conf
          fi

          # iStoreUI
          echo "src-git istoreui https://github.com/linkease/istore-ui.git;main" >> feeds.conf

          # OpenAppFilter
          echo "src-git appfilter https://github.com/destan19/OpenAppFilter.git;master" >> feeds.conf

          # MosDNS (sbwml 维护版，v5 分支兼容 24.10)
          echo "src-git mosdns https://github.com/sbwml/luci-app-mosdns.git;v5" >> feeds.conf

          # OpenClash (sbwml 维护版，master 分支已验证可用)
          echo "src-git openclash https://github.com/sbwml/luci-app-openclash.git;master" >> feeds.conf

      - name: 更新并安装全部 feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # 解决可能的 dnsmasq 冲突（OpenClash 需要 dnsmasq-full）
      - name: 处理关键依赖冲突
        run: |
          # 如果存在 dnsmasq，替换为 dnsmasq-full
          if grep -q "CONFIG_PACKAGE_dnsmasq=y" .config 2>/dev/null; then
            sed -i 's/CONFIG_PACKAGE_dnsmasq=y/# CONFIG_PACKAGE_dnsmasq is not set/' .config
          fi
          echo "CONFIG_PACKAGE_dnsmasq-full=y" >> .config

      # ===== 缓存加速 =====
      - name: 设置 ccache
        uses: hendrikmuhs/ccache-action@v1.2
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          key: n60pro-immortalwrt
          max-size: 500M

      - name: 缓存源码包
        uses: actions/cache@v5
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          path: dl
          key: ${{ runner.os }}-dl-${{ hashFiles('feeds.conf', 'feeds.conf.default') }}
          restore-keys: |
            ${{ runner.os }}-dl-

      # ===== 配置固件 =====
      - name: 加载高功率模板并启用全部插件
        run: |
          if [ ! -f defconfig/mt7975-ipailna-high-power.config ]; then
            echo "::error::找不到高功率配置文件，请检查仓库是否包含 defconfig/mt7975-ipailna-high-power.config"
            exit 1
          fi
          cp defconfig/mt7975-ipailna-high-power.config .config

          # 强制指定目标设备
          cat >> .config <<EOF
          CONFIG_TARGET_mediatek=y
          CONFIG_TARGET_mediatek_mt7981=y
          CONFIG_TARGET_mediatek_mt7981_DEVICE_mediatek_mt7981-netcore-n60-pro=y
          EOF

          # 启用全部插件（包含中文语言包）
          cat >> .config <<EOF
          CONFIG_PACKAGE_luci-app-adguardhome=y
          CONFIG_PACKAGE_adguardhome=y
          CONFIG_PACKAGE_luci-i18n-adguardhome-zh-cn=y
          CONFIG_PACKAGE_luci-app-openclash=y
          CONFIG_PACKAGE_luci-app-mosdns=y
          CONFIG_PACKAGE_luci-i18n-mosdns-zh-cn=y
          CONFIG_PACKAGE_luci-app-istoreui=y
          CONFIG_PACKAGE_luci-i18n-quickstart-zh-cn=y
          CONFIG_PACKAGE_luci-app-oaf=y
          CONFIG_PACKAGE_luci-i18n-oaf-zh-cn=y
          EOF

          # 展开依赖（唯一的一次 defconfig）
          make defconfig

          # 检查插件是否成功选中
          echo "::group::插件状态检查"
          failed=0
          for pkg in luci-app-adguardhome luci-app-openclash luci-app-mosdns luci-app-istoreui luci-app-oaf; do
            if grep -q "CONFIG_PACKAGE_${pkg}=y" .config; then
              echo "✅ $pkg 已启用"
            else
              echo "❌ $pkg 未启用（可能因依赖不满足）"
              failed=1
            fi
          done
          if [ $failed -eq 1 ]; then
            echo "::error::部分插件未成功启用，请检查 feeds 是否更新完整或依赖冲突"
            exit 1
          fi
          echo "::endgroup::"

      # ===== 编译 =====
      - name: 编译固件（稳定模式）
        run: |
          make -j2 V=s

      # ===== 上传固件 =====
      - name: 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-n60pro-full
          path: bin/targets/mediatek/mt7981/
